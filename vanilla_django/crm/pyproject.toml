[project]
name = "crm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0",
    "django-browser-reload>=1.21.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
